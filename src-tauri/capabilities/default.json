{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — allows updater plugin and basic window operations",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
